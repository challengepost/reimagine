<% if current_page.last? %>
  <li class="next arrow unavailable">
    <%= raw(t 'views.pagination.next') %>
  </li>
<% else %>
  <li class="next arrow">
    <%= link_to raw(t 'views.pagination.next'), url, :rel => 'next', :remote => remote %>
  </li>
<% end %>
