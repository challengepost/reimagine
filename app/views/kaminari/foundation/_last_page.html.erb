<% if current_page.last? %>
  <li class="last arrow unavailable">
    <%= raw(t 'views.pagination.last') %>
  </li>
<% else %>
  <li class="last arrow">
    <%= link_to raw(t 'views.pagination.last').html_safe, url, {:remote => remote} %>
  </li>
<% end %>
